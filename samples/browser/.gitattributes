# This files is huge and doesn't contain anything really interesting, so exclude
# it from diffs by default (this relies on having run something like
# "git config --global diff.generated.command 'echo Diff of "$1" suppressed'")
assets/css/bootstrap.css diff=generated
assets/js/bootstrap.js diff=generated
assets/js/frontpanel-ws.js diff=generated
assets/js/jquery.js diff=generated
